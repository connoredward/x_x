import styles from './styles.scss';

const App: React.FunctionComponent = () => (
  <div className={styles.test}>
    <h1 className={styles.header}>Next.js</h1>
    <p className={styles.sub}>with Tailwind CSS</p>
  </div>
);

export default App;
